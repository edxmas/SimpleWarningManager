# Configuration

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
play.crypto.secret="E27D^[_<Lpt0vjad]de;3/i;tx3gpRmG4Byof/3nahO/dIo9gbsMWut1w3xg[>9W"

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
# db.default.driver=org.h2.Driver
# db.default.url="jdbc:h2:mem:play"
db.default.jndiName=DefaultDS
db.default.driver=org.postgresql.Driver
db.default.url="jdbc:postgresql://localhost:5432/linuxday2015"
db.default.username="linuxday"
db.default.password="l1nuxd@y2015"

//db.default.autocommit=false

# Evolutions
# ~~~~~
# You can disable evolutions if needed
#evolutionplugin=disabled
# evolutionplugin=enabled

# JPA configuration
# ~~~~~
# You can declare as many Ebean servers as you want.
# By convention, the default server is named `default`
jpa.default=defaultPersistenceUnit

# Assets configuration
# ~~~~~
# "assets.cache./public/stylesheets/bootstrap.min.css"="max-age=3600"

# SMTP configuration TODO: modificare email/pwd
smtp.host="smtp.gmail.com"
smtp.port=587
smtp.ssl=true
smtp.user=""
smtp.password=""

# Email configuration
email.from="noreply@base-project.org"
email.replyTo="support@base-project.org"

# Session configuration
application.session.maxAge=1h
application.session.secure=true
application.session.sendOnlyIfChanged=true

# WS Configuration
# ~~~~~
ws.timeout.connection=60